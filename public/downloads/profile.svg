<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 800" fill="none">
  <!-- Fundo violeta -->
  <rect width="800" height="800" fill="#7C3AED"/>
  <!-- Ícone E centralizado e grande -->
  <!-- Barra vertical esquerda -->
  <rect x="220" y="190" width="72" height="420" rx="36" fill="white"/>
  <!-- Barra superior (longa) -->
  <rect x="220" y="190" width="360" height="72" rx="36" fill="white"/>
  <!-- Barra central (curta — hierarquia) -->
  <rect x="220" y="364" width="248" height="72" rx="36" fill="white"/>
  <!-- Barra inferior (longa) -->
  <rect x="220" y="538" width="360" height="72" rx="36" fill="white"/>
</svg>
